<h1><%= @student.first_name %></h1>
<h1><%= @student.last_name %></h1>
<h1><% @student.addresses.each do |address|%></h1>
  <li>
    <%= "#{address.street}, #{address.city}, #{address.state}, #{address.zip_code}" %>
  </li>
  <%end%>
